package com.example;

public class AppTest {

    public void testApp() {
        // Simple self-contained check using our custom Assertions (no external deps)
        Assertions.assertEquals(1, 1);
    }
}
